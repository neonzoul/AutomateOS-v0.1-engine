def process_workflow(workflow_id: int):
    """
    Task for prove webhooks connection with trigger_workflow webhook.
    """
    print(f"Worker is processing workflow ID: {workflow_id}")
    
    # {To-do week4}

    return f"Successfully processed workflow {workflow_id}"